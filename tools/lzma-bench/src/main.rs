//! Decode-throughput harness. See `docs/benchmarking.md`.
//!
//! Scaffold: the crate has no decoder yet, so this only reports its version.

fn main() {
    println!("lzma-bench (lzma-fast {})", lzma_fast::VERSION);
}
